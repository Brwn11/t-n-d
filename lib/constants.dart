import 'package:flutter/material.dart';
import 'package:logger/logger.dart';

int bottomBarCurrentIndex = 0;
const karthikNumber = 7736422669;
const double appBarHeight = 70;
const underProcessInsideContainerWidth = 80;
const double underProcessInsideContainerHeight = 80;
const double contraHelpImageWidth = 140;
const double containerHeaderHeight = 30;
const double view3SizedBoxHeight = 15;
const double memberDashboard2CircleSize = 30;
Logger logger = Logger();
topContainerTextStyle() => const TextStyle(
      color: Colors.white,
      fontSize: 17,
    );
subscription2PlanNameTextStyle() => const TextStyle(
      color: Colors.white,
      fontSize: 17,
    );
subscriptions2textStyle() => const TextStyle(
      fontSize: 14,
      color: Colors.black,
    );

subscriptionsButtonTextStyle() {
  return const TextStyle(
    color: Colors.white,
    fontSize: 15,
  );
}

gettingStartedTextStyle() {
  return const TextStyle(
    fontSize: 20,
    fontWeight: FontWeight.bold,
    color: Colors.black,
  );
}

letsRegisterTextStyle() {
  return const TextStyle(
    fontSize: 15,
    color: Colors.grey,
  );
}

sizedBox20() {
  return const SizedBox(
    height: 20,
  );
}

sizedBox30() {
  return const SizedBox(
    height: 30,
  );
}

sizedBox10() {
  return const SizedBox(
    height: 10,
  );
}

subscriptionsPageTextStyle() => const TextStyle(
      color: Colors.black,
      fontSize: 14,
    );
datePickerTextStyle() => const TextStyle(
      color: Colors.black,
      fontSize: 14,
    );
datePickerButtonTextStyle() => const TextStyle(
      color: Colors.black,
      fontSize: 17,
    );
view3SizedBoxDivider() => const Divider(
      thickness: 1,
      color: Colors.grey,
      endIndent: 40,
      indent: 40,
    );
memberDashboard2TextStyle() => const TextStyle(
      color: Colors.black,
      fontSize: 11,
    );
const newDesignColor = Color(0xFFcc0033);
const newDesignBackgroundShade = Color.fromRGBO(
  245,
  245,
  245,
  1,
);
